{
  "main": "dist-electron/main.js",
  "scripts": {
    "electron:dev": "concurrently \"npm run dev --prefix frontend\" \"wait-on http://localhost:5173 && tsc -p tsconfig.electron.json && set ELECTRON_START_URL=http://localhost:5173&& electron .\"",
    "electron:build": "npm run build --prefix frontend && tsc -p tsconfig.electron.json && electron-builder"
  },
  "dependencies": {
    "better-sqlite3": "^12.8.0",
    "http-status-codes": "^2.3.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "baseline-browser-mapping": "^2.10.8",
    "concurrently": "^9.2.1",
    "electron": "^41.0.2",
    "electron-builder": "^26.8.1",
    "wait-on": "^9.0.4"
  }
}
